import { shallowMount } from '@vue/test-utils';
import XDataContainer from './XDataContainer.vue';

describe('Test XDataContainer', () => {
  it('Test setup', async () => {
    shallowMount(XDataContainer);
  });
});
