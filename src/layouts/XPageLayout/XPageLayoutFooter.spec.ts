import { shallowMount } from '@vue/test-utils';
import XPageLayoutFooter from './XPageLayoutFooter.vue';

describe('Test XPageLayoutFooter', () => {
  it('Test setup', async () => {
    shallowMount(XPageLayoutFooter);
  });
});
