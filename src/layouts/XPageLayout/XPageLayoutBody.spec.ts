import { shallowMount } from '@vue/test-utils';
import XPageLayoutBody from './XPageLayoutBody.vue';

describe('Test XPageLayoutBody', () => {
  it('Test setup', async () => {
    shallowMount(XPageLayoutBody);
  });
});
