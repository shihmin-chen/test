import { shallowMount } from '@vue/test-utils';
import XModalLayoutHeader from './XModalLayoutHeader.vue';

describe('Test XModalLayoutHeader', () => {
  it('Test setup', async () => {
    shallowMount(XModalLayoutHeader);
  });
});
