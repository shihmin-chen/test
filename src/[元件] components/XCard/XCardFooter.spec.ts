import { render } from '@testing-library/vue';
import XCardFooter from './XCardFooter.vue';

describe('XCardFooter', () => {
  it('Render without error', async () => {
    render(XCardFooter);
  });
});
