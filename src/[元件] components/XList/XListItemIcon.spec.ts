import { render } from '@testing-library/vue';
import XListItemIcon from './XListItemIcon.vue';

describe('XListItemIcon', () => {
  it('Render without error', async () => {
    render(XListItemIcon);
  });
});
