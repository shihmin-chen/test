# ⚠️ Theme CSS 衝突解決指南

## 🐛 問題說明

當從 GitHub 複製這個元件庫到 Figma Make 專案時，會有**兩個 theme.css 檔案**同時被載入，造成 CSS 變數衝突。

### 衝突的檔案

1. **`/src/styles/theme.css`** (Figma Make 預設)
   - Figma Make 預設生成的 theme
   - 通常是白色背景

2. **`/src/components/react-components/theme.css`** (本元件庫)
   - 我們的設計系統
   - 灰色背景系統

---

## ✅ 解決方案（必須執行！）

### 步驟 1：停用 Figma Make 預設的 theme.css

**檔案：** `/src/styles/index.css`

**修改前：**
```css
@import './fonts.css';
@import './tailwind.css';
@import './theme.css';
```

**修改後：**
```css
@import './fonts.css';
@import './tailwind.css';
/* @import './theme.css'; */  /* 停用：改用元件庫的 theme.css */
```

❗ **重要：** 一定要註解掉這一行！

---

### 步驟 2：確認元件庫 theme.css 已載入

元件庫的 `index.tsx` 會自動載入 `all.css`，而 `all.css` 會載入 `theme.css`。

**不需要手動做任何事！** ✅

```tsx
// 只要這樣導入，theme.css 就會自動載入
 import { Button, Input, Card } from './components/react-components';
```

---

## 🎨 正確的顏色值

修正後，系統應使用這些顏色：

```css
:root {
  /* 背景顏色 */
  --background: #F5F5F5;           /* 深灰色 - 頁面背景 */
  --card: #F2F2F2;                 /* 淺灰色 - 卡片背景 */
  --popover: #F2F2F2;              /* 淺灰色 - 下拉選單 */
  
  /* Input 顏色 */
  --input: #F2F2F2;                /* Input 填充後 */
  --input-background: rgba(26, 26, 26, 0.16);  /* Input fill 樣式 */
  
  /* 其他顏色 */
  --foreground: #1A1A1A;           /* 文字顏色 */
  --primary: #0067CC;              /* 主要顏色 */
  --border: rgba(26, 26, 26, 0.3); /* 邊框 */
}
```

---

## 🔍 如何驗證修正成功

### 1. 檢查頁面背景

打開開發者工具，檢查 `<body>` 或最外層 `<div>`：

```css
background-color: rgb(245, 245, 245);  /* ✅ 深灰色 */
```

**如果看到：**
- `rgb(255, 255, 255)` → ❌ 還在使用 Figma Make 預設的白色
- `rgb(204, 219, 200)` → ❌ 還在使用舊版本的綠色

→ **請確認有注解掉 `/src/styles/index.css` 中的 `@import './theme.css';`**

### 2. 檢查卡片背景

檢查任一 `.bg-card` 或 Card 元件：

```css
background-color: rgb(242, 242, 242);  /* ✅ 淺灰色 */
```

### 3. 檢查 Input fill

檢查任一有 `fill` 屬性的 Input：

```css
background-color: rgba(26, 26, 26, 0.16);  /* ✅ 半透明深灰 */
```

---

## 📝 快速檢查清單

複製元件庫到新專案後，請確認：

- [ ] 已注解 `/src/styles/index.css` 中的 `@import './theme.css';`
- [ ] 已從 `./components/react-components` 導入元件
- [ ] 頁面背景為 **深灰色** (#F5F5F5)
- [ ] 卡片背景為 **淺灰色** (#F2F2F2)
- [ ] Input fill 在卡片上有明顯對比
- [ ] 沒有看到白色或綠色背景

---

## 👥 給 AI 的指令

當使用 Figma Make AI 生成專案時，告訴它：

```
請從 GitHub repo https://github.com/shihmin-chen/test 
的 react-components 資料夾讀取所有檔案並複製到 src/components/react-components/

然後修改 /src/styles/index.css，注解掉：
@import './theme.css';

因為我們要使用元件庫的 theme.css，避免衝突。

然後參考 STYLING_GUIDELINES.md 來正確使用元件。
```

---

## ⚠️ 常見錯誤

### 錯誤 1：忘記注解 theme.css

**症狀：** 背景是白色或綠色，不是灰色

**解決：** 檢查 `/src/styles/index.css`，確認有注解：
```css
/* @import './theme.css'; */
```

### 錯誤 2：沒有導入元件庫

**症狀：** 元件沒有樣式

**解決：** 確認導入路徑正確：
```tsx
import { Button, Input, Card } from './components/react-components';
// ✅ 正確
```

### 錯誤 3：元件庫路徑錯誤

**症狀：** 無法找到模組

**解決：** 根據你的檔案結構調整路徑：
```tsx
// 如果在 /src/app/App.tsx
import { Button } from './components/react-components';

// 如果在 /src/app/pages/Home.tsx
import { Button } from '../components/react-components';
```

---

## 📚 相關文件

- **STYLING_GUIDELINES.md** - 樣式使用指南（Input fill, Select theme 選擇）
- **README.md** - 元件使用說明
- **theme.css** - 設計系統 CSS 變數

---

## 🛠️ 設計系統調整

如果你需要自訂顏色，**只修改這個檔案**：

**`/src/components/react-components/theme.css`**

```css
:root {
  /* 修改這些值 */
  --background: #F5F5F5;     /* 你的頁面背景色 */
  --card: #F2F2F2;           /* 你的卡片背景色 */
  --primary: #0067CC;        /* 你的主要顏色 */
  /* ... 其他變數 */
}
```

⚠️ **不要修改** `/src/styles/theme.css` (已被停用)

---

## 🎯 總結

### 必須做的事

1. ✅ 注解 `/src/styles/index.css` 中的 `@import './theme.css';`
2. ✅ 從 `./components/react-components` 導入元件
3. ✅ 參考 `STYLING_GUIDELINES.md` 使用 fill 和 theme

### 不要做的事

1. ❌ 不要取消注解 `/src/styles/theme.css`
2. ❌ 不要手動導入 theme.css
3. ❌ 不要修改 `/src/styles/theme.css`

---

**更新時間：** 2026-01-12  
**狀態：** ✅ 修正完成
