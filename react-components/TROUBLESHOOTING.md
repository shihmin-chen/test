# 🔧 常見問題解決指南

> 快速查找和解決常見問題

---

## 🐛 問題 1：背景是白色或綠色

**原因：** 沒有註解 `/src/styles/index.css` 中的 `@import './theme.css';`

**解決：**

1. 打開 `/src/styles/index.css`
2. 修改為：

```css
@import './fonts.css';
@import './tailwind.css';
/* @import './theme.css'; */  /* ⚠️ 註解這行 */
```

3. 重新啟動開發伺服器

---

## 🐛 問題 2：Card 或 Modal 透明

**原因：** Figma 導入代碼包含透明背景

**檢查：** 搜尋專案中是否有：
- `bg-[rgba(...,0)]`
- `bg-transparent`
- `opacity: 0` 或 `opacity-0`

**解決：**

```tsx
// ❌ 錯誤
<div className="bg-[rgba(255,255,255,0)]">
  <Card>...</Card>
</div>

// ✅ 修正 - 移除透明背景
<div>
  <Card>...</Card>
</div>

// ✅ 或者改用正確的背景色
<div className="bg-background">
  <Card>...</Card>
</div>
```

**批量修復：**
使用搜尋和替換：
- 搜尋：`bg-\[rgba\(.*,\s*0\)\]`
- 替換：`bg-background` 或空白

---

## 🐛 問題 3：組件沒有樣式

**原因：** CSS 沒有載入

**解決：** 確認從 `'./components/react-components'` 導入：

```tsx
// ✅ 正確
import { Button, Card } from './components/react-components';

// ❌ 錯誤
import { Button } from './components/react-components/Button.tsx';
```

---

## 📝 最佳實踐

### 1. 移除 Figma 導入的透明背景

```tsx
// 移除 bg-[rgba(...,0)]
<div className="p-4"> {/* ✅ 只保留需要的 utility */}
  <Card>...</Card>
</div>
```

### 2. 使用 CSS 變數

```css
/* ✅ 組件庫已正確使用 */
.card-container {
  background-color: var(--card);  /* 不用 !important */
}
```

### 3. 讓樣式可覆蓋

```tsx
{/* ✅ 需要時才覆蓋 */}
<Card className="bg-white">...</Card>
```

---

## ⚠️ 避免使用 !important

**原始 Vue 組件完全沒有使用 `!important`**

React 組件也不應該使用 `!important`，因為：
- ❌ 破壞 CSS 層疊規則
- ❌ 讓樣式難以覆蓋
- ❌ 是 CSS 反模式

**正確做法：** 使用 CSS 變數 + 移除透明背景

---

## 🔍 驗證方法

### 使用開發者工具

1. 右鍵點擊 Card 或 Modal
2. 選擇「檢查」
3. 查看 **Computed** 標籤頁
4. 找到 `background-color`

**正確：**
```css
background-color: rgb(242, 242, 242);  /* ✅ 淺灰色 */
```

**錯誤：**
```css
background-color: rgba(255, 255, 255, 0);  /* ❌ 透明 */
background-color: transparent;              /* ❌ 透明 */
```

---

## 🎯 檢查清單

- [ ] `/src/styles/index.css` 中的 `@import './theme.css';` 已註解
- [ ] 沒有 `bg-[rgba(...,0)]` className
- [ ] Card 背景是淺灰色 (#F2F2F2)，不透明
- [ ] Modal 背景是淺灰色，不透明
- [ ] 開發者工具顯示正確的 background-color

---

**更新時間：** 2026-01-12  
**版本：** 3.0.0 (精簡版)
