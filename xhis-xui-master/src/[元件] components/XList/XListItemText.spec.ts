import { render } from '@testing-library/vue';
import XListItemText from './XListItemText.vue';

describe('XListItemText', () => {
  it('Render without error', async () => {
    render(XListItemText);
  });
});
