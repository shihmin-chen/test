import { render } from '@testing-library/vue';
import XListSubheader from './XListSubheader.vue';

describe('XListSubheader', () => {
  it('Render without error', async () => {
    render(XListSubheader);
  });
});
