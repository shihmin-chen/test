import { shallowMount } from '@vue/test-utils';
import XPageLayoutHeader from './XPageLayoutHeader.vue';

describe('Test XPageLayoutHeader', () => {
  it('Test setup', async () => {
    shallowMount(XPageLayoutHeader);
  });
});
