import { shallowMount } from '@vue/test-utils';
import XPageLayout from './XPageLayout.vue';

describe('Test XPageLayout', () => {
  it('Test setup', async () => {
    shallowMount(XPageLayout);
  });
});
