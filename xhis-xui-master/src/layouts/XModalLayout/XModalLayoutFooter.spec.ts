import { shallowMount } from '@vue/test-utils';
import XModalLayoutFooter from './XModalLayoutFooter.vue';

describe('Test XModalLayoutFooter', () => {
  it('Test setup', async () => {
    shallowMount(XModalLayoutFooter);
  });
});
