import { shallowMount } from '@vue/test-utils';
import XModalLayout from './XModalLayout.vue';

describe('Test XModalLayout', () => {
  it('Test setup', async () => {
    shallowMount(XModalLayout);
  });
});
