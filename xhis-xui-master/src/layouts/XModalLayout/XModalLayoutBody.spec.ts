import { shallowMount } from '@vue/test-utils';
import XModalLayoutBody from './XModalLayoutBody.vue';

describe('Test XModalLayoutBody', () => {
  it('Test setup', async () => {
    shallowMount(XModalLayoutBody);
  });
});
